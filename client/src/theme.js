import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#365f3d',
    },
    secondary: {
      main: '#cfebc0',
    },
    tertiary: {
      main: '#f2bac9',
    },
  }
});

export default theme;
